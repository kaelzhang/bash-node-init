# bash-node-init

Initialize a node.js project (personal use)

## Installation

Open ~/.bash_profile

```bash
if [[ -f /path/to/init.sh ]]; then
    source /path/to/init.sh
fi
```

## Usage

```bash
init repo-url
```